# CathayIndex 发布流程

> 每次发新版照着走一遍，10 分钟搞定。

## 0. 前提

- 本机 Python 3.10+（打包用 `py -3`）
- 依赖：`tkinterdnd2`（可选拖放，`py -3 -m pip install tkinterdnd2`）
- 图标：`app.ico` 已在仓库里

## 1. 定版本号

改 `indexer.py` 里的 `APP_VERSION`（如 `v1.0.0` → `v1.0.1`）。
界面标题会自动跟着变（`gui.py` 用 `IDX.APP_TITLE + IDX.APP_VERSION`）。

## 2. 自检（必须）

```bat
python indexer.py --selftest      :: 引擎
python gui.py --selftest          :: 引擎 + 界面
```

打包后**再跑一次 exe 自检**（这一步能抓到「运行时数据文件没打进去」这类问题）：

```bat
dist\Release\CathayIndex.exe --selftest
type dist\Release\_selftest.txt
```

## 3. 一键打包

```bat
发版.bat
:: 或者
py -3 fab.py
:: 只重算校验值/发行说明（不重新打包）
py -3 fab.py --skip-build
```

产物（都在 `dist\Release\`）：

- `CathayIndex.exe` —— 单文件版
- `校验值.txt` —— 大小 + SHA256
- `发行说明_vX.Y.Z.md` —— 骨架（**手写过的正文不会被覆盖**）

## 4. 便携版 zip（可选）

把 `建库.bat` + `runtime\` + `app.ico` 打成一个 zip，给不想用 exe 的人：

```
CathayIndex-vX.Y.Z-portable.zip
  ├── 建库.bat
  ├── runtime\          （约 40 MB，自带 Python，双击 建库.bat 即用）
  └── app.ico
```

> `runtime\` 的裁剪办法：拷一份 Python 3.10+ 的独立解释器目录，只保留标准库、
> `tcl\`、`DLLs\`（含 `_tkinter.pyd`、`_sqlite3.pyd`）与 `Lib\site-packages\tkinterdnd2\`，
> 删掉 `Doc\`（占几十 MB）。然后 `runtime\python.exe -c "import tkinter, sqlite3, tkinterdnd2"` 验一下。

## 5. 上传

1. **百度网盘**（单一入口，给普通用户）：上传 exe / 便携版 zip，把分享链接（密码 2026）回填到 README 的「下载」表
2. **GitHub Releases**：

```bat
gh release create vX.Y.Z "dist\Release\CathayIndex.exe" ^
   --title "CathayIndex vX.Y.Z" --notes-file "dist\Release\发行说明_vX.Y.Z.md"
```

3. 回填 README：更新「下载」表链接、更新日志加一条

## 6. 记住几条坑

- **`--name` 绝不能和源码目录同名**：PyInstaller 会在打包最后清掉输出目录，
  若 `--name CathayIndex` 而源码里也有同名目录，源码会被删掉。本仓库源码是
  `indexer.py` / `gui.py` 顶层文件，不受影响，但仍建议 `--distpath dist`。
- **运行时数据文件**（词典、图标、css…）要用 `--add-data` / `--collect-all` 显式带上，
  否则源码跑得通、exe 跑不通。
- **中文路径**没问题，但 `.bat` 里**不要写中文**（cmd 按 GBK 解析会乱），本仓库的
  `发版.bat` 是纯 ASCII。
